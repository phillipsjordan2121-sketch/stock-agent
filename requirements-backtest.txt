# Phase 0 backtest dependencies (separate from the live pipeline).
numpy>=1.24
requests>=2.28      # imported transitively by main.py (universe source)
anthropic>=0.39     # imported transitively by main.py (universe source)
